.codegraph/
.atl/
bin/
# `go build ./cmd/vcsentinel` writes the executable to the repository root, not bin/.
# Leading slashes keep the ignore rule from matching the cmd/vcsentinel/ package.
/vcsentinel
/vcsentinel.exe
# Preserve pre-cutover root binaries without making them part of the product.
/sentinel
/sentinel.exe
