# Doctor hook validation

Status: complete

## Goal

Make `vcsentinel doctor` recognize the current marked direct pre-commit hook generated by `vcsentinel init`, while preserving support for the legacy direct hook shape and the stable-binary safety check. Investigate the reported Claude warning without weakening probe failures or hiding real warnings.

## Evidence before implementation

- `./bin/1.0.0/vcsentinel check` exits 0 and reports the worktree volume as SMALL.
- `./bin/1.0.0/vcsentinel doctor` exits 0 but reports WARN for `codegraph.worktree_clean` and `hook.pre-commit`; the Claude resolution and probe rows are OK in the reproduced output.
- `.git/hooks/pre-commit` contains the marked direct hook generated by the current installer and points at `/usr/local/bin/vcsentinel`.
- `internal/doctor/doctor.go` parses only the legacy two-line hook shape, so it rejects the current installed shape.

## Scope

Owned paths:

- `internal/doctor/doctor.go`
- `internal/doctor/doctor_test.go`
- `odd/tasks/doctor-hook-validation.md`

Do not modify the existing unrelated dirty files or `.git/hooks/pre-commit` during source implementation.

## Acceptance matrix

| ID | Criterion | Evidence | Owner | Status |
|---|---|---|---|---|
| C-01 | A marked direct hook generated by init is accepted and its executable target is extracted. | `go test ./internal/doctor -run TestMarkedDirectHook -count=1`: RED before production code, then GREEN. | Writer | met |
| C-02 | Legacy direct hook parsing and versioned-binary rejection remain unchanged. | `go test ./internal/doctor -count=1` passed in the writer worktree. | Writer | met |
| C-03 | Claude probe behavior is traced; a real Claude warning is either reproduced with evidence or shown absent in the current report. | `go run ./cmd/vcsentinel doctor` and rebuilt `./bin/1.0.0/vcsentinel doctor` both report Claude resolution and probe rows as OK; the remaining warning is CodeGraph worktree cleanliness. | Coordinator | met |
| C-04 | Focused, package, build, vet, and final doctor checks pass after rebuilding the binary. | Focused RED/GREEN, `go test -count=1 ./internal/doctor`, `go test -count=1 ./cmd/vcsentinel`, `go vet ./...`, `go build ./...`, exact release build, both doctor runs, and `vcsentinel check` all exited 0. | Coordinator | met |
| C-05 | Existing unrelated worktree changes remain untouched and unstaged. | Final check reported no staged paths; only the two source files and this task document are task-owned. | Coordinator | met |

## Tasks

1. Add a focused failing regression for the current marked direct hook shape. **Done:** writer observed RED before production code and GREEN after the fix.
2. Update the doctor hook parser minimally, preserving legacy parsing and safety checks. **Done:** current marked direct hooks are accepted; legacy direct hooks and versioned-binary rejection remain covered.
3. Independently verify the Claude probe report, touched package, build, vet, and the rebuilt doctor command. **Done:** Claude is healthy; all requested checks passed.

## Delivery

The user subsequently authorized commit and push delivery. This task record was delivered with work-unit commit `636dd2a` (`fix(doctor): accept marked direct pre-commit hooks`) and pushed to `origin/main`; no pull request is being created.

## Non-goals

- Do not change Claude credentials, models, timeouts, or agent probe semantics without a reproduced Claude-specific failure.
- Do not reinstall or rewrite the repository hook as part of this source fix.
- Do not commit or publish without a separate explicit user request.
